<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="Chatterloop"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:roundIcon="@mipmap/ic_launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- FCM: the channel a background/killed "notification" message is
             posted to by the system. Matches PushNotificationService's local
             notifications channel id so foreground (app-shown) and background
             (OS-shown) notifications land in the same, high-importance channel. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="chatterloop_messages_v2" />
        <!-- Status-bar icon for OS-shown notifications. Without this the FCM
             SDK falls back to the app's launcher icon, which Android masks to
             its alpha channel and renders as a solid white blob. Same drawable
             the app-shown notifications use (NotificationRenderer._smallIcon)
             so both paths look identical. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_chatterloop" />
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- record's AudioRecorder needs this to capture voice messages. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Video calling (M7) - flutter_webrtc requests this itself at
         runtime via its own PermissionUtils the same way it already does
         for RECORD_AUDIO (see GetUserMediaImpl.java), so no
         permission_handler dependency is needed - this manifest
         declaration is only what makes that runtime request possible. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- FCM push notifications: Android 13+ (API 33) requires this runtime
         permission to DISPLAY notifications. Requested at runtime by
         PushNotificationService.requestPermission(); it's a no-op / auto-
         granted on Android 12 and below. Registration works without it -
         only display is gated. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher's canLaunchUrl/launchUrl need this on Android 11+
             (API 30+) package visibility rules, or link previews/embeds can
             fail to open even once the plugin itself is registered. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
    </queries>
</manifest>
